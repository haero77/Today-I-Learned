package com.example.demo.user.domain;

import org.assertj.core.api.Assertions;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

class UserTest {

    @DisplayName("User 는 UserCreate 객체로 생성할 수 있다.")
    @Test
    void createUserByUserCreate() {
        // given

        // when

        // then
//        Assertions.assertThat()
    }

    @DisplayName("User 는 UserUpdate 객체로 데이터를 업데이트할 수 있다.")
    @Test
    void updateUserByUserUpdate() {
        // given

        // when

        // then
//        Assertions.assertThat()
    }
    
    @DisplayName("User 는 로그인 할 수 있고, 로그인 시 마지막 로그인 시간이 변경된다.")
    @Test
    void userLogin() {
        // given
            
        // when
        
        // then
        Assertions.assertThat()
    }
    
    @DisplayName("유효한인증 코드로 계정을 활성화 할 수 있다.")
    @Test
    void () {
        // given
            
        // when
        
        // then
        Assertions.assertThat()
    }
    
    @DisplayName("잘못된 인증 코드로 계정을 활성화 하려하면 에러를 던진다.")
    @Test
    void () {
        // given
            
        // when
        
        // then
        Assertions.assertThat()
    }

}