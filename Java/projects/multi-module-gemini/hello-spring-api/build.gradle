tasks.named("bootJar") {
    enabled = true // turn on 'bootJar' task to generate an Executable-Jar
}

tasks.named("jar") {
    enabled = false
}

dependencies {
    implementation(project(":support:logging"))

    implementation ("org.springframework.boot:spring-boot-starter-web")
}