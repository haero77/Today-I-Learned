# 2.2 첫 번째 뷰 애플리케이션 만들기

## 2.2.2 NPM을 사용한 뷰 애플리케이션 만들기

```
npm create vue@latest
```
- 프로젟트 생성 후, npm install 명령어로 의존성 설치

```
npm install 
```

- npm install 이후 node_modules 폴더가 생성됨(npm install로 설치되므로, git 으로 관리 불필요)

```
npm run dev
```

